table_name("users")
schema([
    Column("uid", BIGINT),
    Column("gid", BIGINT),
    Column("username", TEXT),
    Column("description", TEXT),
    Column("directory", TEXT),
    Column("shell", TEXT),
])
implementation("users@genUsers")
