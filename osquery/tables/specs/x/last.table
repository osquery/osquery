table_name("last")
description("System logins and logouts.")
schema([
    Column("username", TEXT),
    Column("tty", TEXT),
    Column("pid", INTEGER),
    Column("type", INTEGER),
    Column("time", INTEGER),
    Column("host", TEXT),
])
implementation("last@genLastAccess")
