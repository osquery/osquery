table_name("listening_ports")
description("Processes which are listening on network ports.")
schema([
    Column("pid", INTEGER),
    Column("port", INTEGER),
    Column("protocol", INTEGER),
    Column("family", INTEGER),
    Column("address", TEXT),
])
implementation("listening_ports@genListeningPorts")
