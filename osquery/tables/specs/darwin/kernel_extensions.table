table_name("kernel_extensions")
schema([
    Column("idx", INTEGER),
    Column("refs", INTEGER),
    Column("size", BIGINT),
    Column("wired", BIGINT),
    Column("name", TEXT),
    Column("version", TEXT),
    Column("linked_against", TEXT),
])
implementation("kextstat@genKernelExtensions")
