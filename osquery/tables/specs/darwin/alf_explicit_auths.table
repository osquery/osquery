table_name("alf_explicit_auths")
description("Explicitly authorized programs which are allowed to perform networking activity usually disallowed by the application level firewall.")
schema([
    Column("process", TEXT),
])
implementation("firewall@genALFExplicitAuths")
