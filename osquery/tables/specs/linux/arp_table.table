table_name("arp_table")
schema([
    Column(name="address", type="std::string"),
    Column(name="mac", type="std::string"),
    Column(name="interface", type="std::string"),
])
implementation("arp@genArpTable")
