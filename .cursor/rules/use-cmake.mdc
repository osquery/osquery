---
description: CMake build configuration
alwaysApply: true

- This project uses CMake.
- When building, use cmake commands.
- For make commands, set paralellism (using -j) to the number of available CPU cores on the current system.
- Always run make commands in the build/ subdirectory in the root of the repository.
- When adding new files, edit the appropriate CMakeLists.txt
---
