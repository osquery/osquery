/**
 *  Copyright (c) 2014-present, Facebook, Inc.
 *  All rights reserved.
 *
 *  This source code is licensed under both the Apache 2.0 license (found in the
 *  LICENSE file in the root directory of this source tree) and the GPLv2 (found
 *  in the COPYING file in the root directory of this source tree).
 *  You may select, at your option, one of the above-listed licenses.
 */

/*
** This file is generated. Do not modify it manually!
*/

#include <osquery/events.h>
#include <osquery/logger.h>
#include <osquery/tables.h>

{% if foreign %}
#define DECLARE_TABLE_FOREIGN_STUB
{% for table in tables %}
#include <generated/tables/tbl_{{table.name}}_defs.hpp>
{% endfor %}
{% endif %}

namespace osquery {

{% if foreign %}
  void registerForeignTables() {
  {% for table in tables %}
    tbl_{{table.name}}_register_stub();
  {% endfor %}
  }
{% else %}
  class RegisterLinkedTables{{category}} {
  public:
    RegisterLinkedTables{{category}}() {
    {% for table in tables %}
    {% if table.is_blacklisted %}
        // table {{table.name}} is blacklisted
    {% else %}
        extern void tbl_{{table.name}}_register();
        tbl_{{table.name}}_register();
    {% endif %}
    {% endfor %}
    }
  };
  static const auto doAutoReg = RegisterLinkedTables{{category}}();
{% endif %}

} // namespace
